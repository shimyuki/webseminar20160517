package window.setting {	import flash.display.*;	import flash.geom.*;	import common.*;	import window.*;	import flash.text.*;	import flash.events.*;	//import flash.net.*;	//import caurina.transitions.*;	import window.chat.ChatContainer;	// 講師用、MemberSettingContentsの個別チャットの部分(Drawerのコンテンツ)	public class MemberChat extends Sprite {				static public const H = 400;		static public const PAD = 8;				//private var m_uid:String;		private var m_base:Shape;		private var m_chatCon:ChatContainer = null;				//private var m_nc:NetConnection;		public function MemberChat() {			//m_uid = uid;						m_base = Shape( addChild( new Shape()));			m_base.graphics.beginFill( 0xf0f0f0);			m_base.graphics.drawRect( 0, 0, 1, H);			m_base.graphics.endFill();						/*m_chatCon = ChatContainer( addChild( new ChatContainer( 100, H - PAD * 2, "chat", uid)));			m_chatCon.setViewHeight( H - PAD * 2);			m_chatCon.x = PAD;			m_chatCon.y = PAD;*/		}		public function setChatCon( chatCon:ChatContainer):void {			if( m_chatCon != null) {				if( contains( m_chatCon)) removeChild( m_chatCon);			}						m_chatCon = chatCon;			m_chatCon.setViewHeight( H - PAD * 2);			m_chatCon.x = PAD;			m_chatCon.y = PAD;						addChild( m_chatCon);		}		/*public function initSo( nc:NetConnection):void {			m_nc = nc;			m_chatCon.initSo( Main.CONF.SO_NAME_CHAT_STU, m_nc);		}*/				/*// クイック一覧の個別チャットのポップアップ用に、MemberSettingContainerから呼ばれる		public function getChatContainer(): ChatContainer {			return m_chatCon;		}*/		/*public function setEnabled( b:Boolean):void {			m_chatCon.setEnabled( b);		}*/		public function setViewWidth( w:Number):void {			m_base.scaleX = w;			if( m_chatCon) m_chatCon.setViewWidth( w - PAD * 2);		}		function alertDialog( str:String) {			Main.addErrMsg( "MemberChat:" + str);		}	}}