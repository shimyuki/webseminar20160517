package window.video.list.parts {	import flash.display.*;	import flash.geom.*;	import common.*;	import window.*;	import flash.text.*;	import flash.events.*;	import fl.controls.CheckBox;	import flash.net.*;	import flash.media.*;	import caurina.transitions.*;	import window.video.VoiceMeter;	import window.video.VolumeCtl;	// 受講生一覧のメンバー表示コンテナのピックアップサムネイル表示のコンテナ	public class ThumbCon_all extends ThumbCon {				public function ThumbCon_all( uid:String) {			super( uid);		}		/*		override public function initSo( nc:NetConnection) : void {						super.initSo( nc);						// このコンテナが自分自身でないなら、ストリームを受信。			if( m_uid != Main.CONF.UID) {				m_receive_ns = new MyNetStream( m_nc, m_uid);				startReceive();			}		}		*/	}}